<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="admin">

	<!-- 유저 리스트 -->
	<select id="userList" resultType="hashmap">
	<![CDATA[
		select * from members
		where m_del_gb='N'
	]]>
	</select>
	
	<!-- 유저 상세정보 -->
	<select id="selectUserDetail" resultType="hashmap">
	<![CDATA[
		select
			m_name,
			m_pw,
			m_email,
			m_cellphone,
			m_reg_date,
			m_mod_date,
			m_own_num,
			m_type,
			m_del_gb
		from
			members
		where
			m_id = #{m_id}
	]]>
	</select>
	
	<!-- 유저 정보 수정 -->
	<update id="modifyUser">
	<![CDATA[
		update members set
			m_name = #{m_name},
			m_pw = #{m_pw},
			m_email = #{m_email},
			m_cellphone = #{m_cellphone},
			m_reg_date = #{m_reg_date},
			m_mod_date = #{m_mod_date},
			m_own_num = #{m_own_num},
			m_type = #{m_type},
			m_del_gb = #{del_gb}
		where
			m_id = #{m_id}
	]]>
	</update>
	
	<!-- 유저 정보 삭제 -->
	<delete id="deleteUser">
	<![CDATA[
		update members set
			m_del_gb = #{del_gb}
		where 
			m_id = #{m_id} and m_del_gb='Y'
	]]>
	</delete>
	
	<!-- 유저 검색 -->
	<select id="selectUserSearch" resultType="hashmap"
		parameterType="hashmap">
		select * from members
		where (
		<trim prefixOverrides="AND">
			<if test="m_type != null">
				<if test="m_type == 0">
					m_type =0
				</if>
				<if test="m_type == 1">
					m_type=1
				</if>
			</if>
			<if test="m_del_gb == 'Y'.toString()">
				AND m_del_gb = 'Y'
			</if>
			<if test="m_del_gb != 'Y'.toString()">
				AND m_del_gb = 'N'
			</if>
			<choose>
				<when test="searchType == 'I'.toString()">
					AND (m_id like '%' || #{keyword} || '%')
				</when>
				<when test="searchType == 'N'.toString()">
					AND (m_name like '%' || #{keyword} || '%')
				</when>
			</choose>
		</trim>
		)
	</select>


</mapper>